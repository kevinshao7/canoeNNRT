# ===============================================
# Examples published in Cheng et al. (2023), ApJS
# ===============================================
cmake_minimum_required(VERSION 3.18)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_PREFIX_PATH "/home/kevinsh/canoe/canoe/libtorch")
#set(CMAKE_PREFIX_PATH "/home/kevinsh/venv/lib/python3.12/site-packages/torch")
#set(TORCH_DIR "/home/kevinsh/venv/lib/python3.12/site-packages/torch/share/cmake/Torch")

#set(TORCH_USE_CUDA OFF CACHE BOOL "Disable CUDA")
#set(CMAKE_CUDA_COMPILER "" CACHE FILEPATH "No CUDA compiler")
#set(CUDA_TOOLKIT_ROOT_DIR "" CACHE PATH "No CUDA toolkit")
find_package(Torch REQUIRED CONFIG)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${TORCH_CXX_FLAGS}")



# 1. Compile dam_break.cpp
if(${HYDROSTATIC})
  if(${EQUATION_OF_STATE} STREQUAL "shallow_yz")
    setup_problem(dam_break)
    setup_problem(test_blast)
    setup_problem(W92)
    setup_problem(test_injection)
    setup_problem(steady_zonal)
  endif()
else()
  # setup_problem(test_adiabat)
  # setup_problem(hs94)
  # setup_problem(hot_jupiter)
  # setup_problem(hot_jupitertest)
  # setup_problem(polar_dry)
  # setup_problem(uranus2e)
  # setup_problem(uranus2f)
  # setup_problem(uranus2g)
  # setup_problem(uranus2h)
  #setup_problem(uranus2i)
  # setup_problem(uranus2j)
  # setup_problem(uranus2l)
  # setup_problem(uranus2m)
  # setup_problem(uranus2n)
  # setup_problem(uranus2o)
  # setup_problem(uranus2p)
  # setup_problem(uranus2q)
  # setup_problem(uranus2r)
  # setup_problem(uranus2r2)
  setup_problem(uranus2s)
  # setup_problem(uranus2t)
  # setup_problem(uranus2u)
  # setup_problem(uranus2v)
  # setup_problem(uranus2w)
  #setup_problem(rturanus)

  add_executable(uranusnn.release uranusnn.cpp ${CMAKE_SOURCE_DIR}/main.cpp)

  set_target_properties(
    uranusnn.release
    PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
               COMPILE_FLAGS ${CMAKE_CXX_FLAGS_${buildu}})

  target_link_libraries(uranusnn.release ${CANOE_LIBRARY_${buildu}} ${TORCH_LIBRARIES})

  target_include_directories(
    uranusnn.release
    PRIVATE ${CMAKE_BINARY_DIR}
            ${TORCH_API_INCLUDE_DIR}
            ${CANOE_INCLUDE_DIR}
            ${CANTERA_INCLUDE_DIR}
            ${EIGEN3_INCLUDE_DIR}
            ${MPI_CXX_HEADER_DIR}
            ${MPI_CXX_INCLUDE_PATH}
            ${NETCDF_INCLUDES}
            ${PNETCDF_INCLUDE_DIR}
            SYSTEM
            ${TORCH_API_INCLUDE_DIR}
            SYSTEM
            ${TORCH_INCLUDE_DIR}
            SYSTEM
            ${EIGEN3_INCLUDE_DIR}
            SYSTEM
            ${DISORT_INCLUDE_DIR}
            SYSTEM
            ${HARP_INCLUDE_DIR}
            SYSTEM
            ${KINTERA_INCLUDE_DIR}
            SYSTEM
            ${SNAP_INCLUDE_DIR})
  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${TORCH_CXX_FLAGS}")
  set_property(TARGET uranusnn.release PROPERTY CXX_STANDARD 17)

endif()

# 2. Copy input file to run directory
file(GLOB inputs *.inp *.dat)
foreach(input ${inputs})
    file(COPY ${input} DESTINATION ${CMAKE_BINARY_DIR}/bin)
endforeach()

